# Параметры окон
 window.main.title = Main section # Заголовок основного окна
 window.main.width = 850          # Ширина основного окна в пикселях
 window.main.cell_size = 7        # Размер клетки (большого пикселя) в основном окне

 window.additional.width = 600    # Ширина дополнительного окна в пикселях
 window.additional.cell_size = 10 # Размер клетки в дополнительном окне

# Параметры шейдера, трассировки луча
 shader.filename = shader.frag
 shader.light_to_color_conversion_coefficient = 1.0 # Чем больше эта константа, тем будет светлее
 shader.samples = 110          # Число запускаемых для каждой клетки экрана лучей
 shader.reflections_amount = 4 # Максимальное число переотражений луча

# Вращение камерой и передвижение
 # Толщина границы у окна, в пределах которой курсор просто телепортируется в центр.
 # Это нужно, чтобы не было резкого скачка, когда мышка входит в окно.
 mouse_border_width = 15

 # Чувствительность мышки: изменение угла поворота камеры при смещении курсора на один пиксель
 mouse_sensitivity = 0.005

 # Чувствительность колёсика: изменение угла поворота камеры при повороте колёсика на единицу
 wheel_sensitivity = 0.1

 # Константа, позволяющая регулировать скорость перемещения камеры
 movement_speed = 2.0

# Параметры виртуальной камеры: влияют на масштаб объектов сцены и угол обзора
 focus_to_matrix_distance = 1.5 # Расстояние от фокуса до матрицы
 matrix_height = 1.0            # Половина высоты матрицы

# Начальное положение камеры
 initial_camera_position.x = 0.0
 initial_camera_position.y = 0.0
 initial_camera_position.z = 0.0
 initial_camera_position.w = 0.0

# Начальные углы поворота камеры в радианах
 initial_camera_position.fi  = 0.0
 initial_camera_position.te  = 0.0
 initial_camera_position.psi = 0.0

# Параметры текста: используется для отображения FPS
 text.font.filename = font.ttf
 text.size = 24
 text.outline_thickness = 2.0

# Разное
 max_fps = 60
 window_title_height = 37 # Высота шапки окон Windows (полоски сверху) в пикселях
 task_bar_height = 60     # Высота панели задач Windows в пикселях
